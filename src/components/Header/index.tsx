import { FC } from 'react';
import { StyledHeader, StyledTile } from './styles';

const Header: FC = () => (
	<StyledHeader>
		<StyledTile>
			Weather
		</StyledTile>
	</StyledHeader>
);

export default Header;
